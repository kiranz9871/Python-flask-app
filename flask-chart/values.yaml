replicaCount: 5

image:
  repository: kiran9871/flask-app
  pullPolicy: IfNotPresent
  tag: "de3da8fcc099b3cfa5eda00e626f5530df8edc67"


service:
  type: NodePort
  port: 5000

config:
  appName: "Production Flask App"
  environment: "production"
  logLevel: "INFO"

serviceAccount:
  create: false

ingress:
  enabled: false

autoscaling:
  enabled: false

livenessProbe:
  httpGet:
    path: /health
    port: http

readinessProbe:
  httpGet:
    path: /health
    port: http

resources: {}